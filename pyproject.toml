[project]
name = "yanki"
version = "0.1.0"
description = "Create anki flashcards from yaml"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "genanki>=0.13.1",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "weasyprint>=69.0",
]
